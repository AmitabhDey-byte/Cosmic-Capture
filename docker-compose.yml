services:
  postgres:
    image: postgres:16-alpine
    container_name: stellar-arena-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: stellar_arena
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - stellar-arena-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d stellar_arena"]
      interval: 5s
      timeout: 5s
      retries: 12

volumes:
  stellar-arena-postgres:
